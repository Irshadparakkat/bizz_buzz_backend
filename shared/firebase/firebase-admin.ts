import * as admin from 'firebase-admin';
// import serviceAccount from 'shared/firebase/firebase-account.json'; // Update the path

// admin.initializeApp({
//   credential: admin.credential.cert(serviceAccount as admin.ServiceAccount),
// });

export { admin };
